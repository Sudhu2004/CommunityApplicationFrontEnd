export const environment = {
  production: true,
  apiUrl: 'https://borough.in'   // no port 8080, Nginx handles it
};

// // environment.ts (local)
// export const environment = {
//   production: false,
//   apiUrl: ''   // empty — proxy handles it
// };